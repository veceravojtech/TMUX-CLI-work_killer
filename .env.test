# SAFE TO COMMIT — disposable throwaway test DB credentials for the CI gate.
# NOT a real secret: the test DB is an ephemeral throwaway container
# (compose default stock_test/stock_test). See docs/architecture/test-environment.md.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=stock_test
DB_USER=stock_test
DB_PASSWORD=stock_test
