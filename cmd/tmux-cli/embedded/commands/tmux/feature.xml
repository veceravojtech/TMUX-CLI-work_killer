<task id="feature" name="Brownfield feature-development orchestrator">
  <objective>Drive ONE feature through an EXISTING system end-to-end — context discovery, architecture recommendation, docs + test strategy, implementation, and validation — by COMPOSING the existing plan/supervisor/investigate primitives. The brownfield analogue of the greenfield task-plan-discover &#8594; task-plan-generate pair. Six stages (0&#8211;5); interactive ONLY at Stage 2; fully autonomous before and after. This file is the SPINE: Stage 0 (capability + topology resolution) is loaded here; Stages 1&#8211;5 are loaded sub-step seams whose shard files arrive with F2&#8211;F6.</objective>

  <input>
    <note>$ARGUMENTS = the feature description / name (a free-text statement of the ONE feature to drive end-to-end). Empty $ARGUMENTS is a hard stop in Stage 0 preflight.</note>
  </input>

  <requirements critical="true">
    <mcp>tmux-cli MCP server REQUIRED (for the plan/supervisor/investigate composition the later stages delegate to).</mcp>
  </requirements>

  <glossary>
    <term name="STAGE 0">Capability + topology resolution (this file's loaded sub-step). Preflight + `tmux-cli rules resolve --signals` capability detection + `tmux-cli rules resolve --kind=convention|code-rules` topology/quality-gate resolution. Autonomous.</term>
    <term name="STAGE 1">Context mapping — locate the existing code, contracts, and seams the feature touches (F2). Autonomous.</term>
    <term name="STAGE 2">Architecture recommendation — present options and confirm the approach WITH the user (F3). The SOLE interactive stage.</term>
    <term name="STAGE 3">Docs + test strategy — record the decision and define the test approach (F4). Autonomous.</term>
    <term name="STAGE 4">Implementation — delegate to /tmux:plan + /tmux:supervisor (F5). Autonomous.</term>
    <term name="STAGE 5">Validation — delegate to /tmux:investigate (F6). Autonomous.</term>
    <term name="CAPABILITY SIGNALS">The `tmux-cli rules resolve --signals --json` fields carried forward from Stage 0: LANG, FRAMEWORK, RUN_TARGET, HAS_DATABASE, HAS_FRONTEND, FRONTEND_MODE, N_AUTH_FLOWS. E2E_RUNNER_AVAILABLE is NOT a Signals field — Stage 0 probes it separately.</term>
    <term name="TOPOLOGY">The resolved source roots, layer names, root namespace, and quality-gate commands read from the convention/code-rule packs `tmux-cli rules resolve` loads for THIS project's stack. Greenfield defaults only when the packs are empty.</term>
  </glossary>

  <llm critical="true">
    <mandate>COMPOSER, NOT ENGINE: /tmux:feature orchestrates the EXISTING primitives — it delegates implementation to /tmux:plan + /tmux:supervisor and validation to /tmux:investigate. NEVER re-implement their planning, dispatch, or verification logic here; reference them.</mandate>
    <mandate>PORTABILITY IS MANDATORY: NEVER hardcode any project-shaped value — source roots, layer names, root namespace, quality-gate commands, lang/framework, ports. Resolve every such value from `tmux-cli rules resolve` / `--signals` / project manifests, or ASK at Stage 2. Greenfield skeleton values are DEFAULTS only. Emit no project name anywhere in this command.</mandate>
    <mandate>INTERACTIVE ONLY AT STAGE 2: Stage 2 (architecture recommendation) is the SOLE stage that may prompt the user. Stages 0, 1, 3, 4, and 5 run autonomously — within them, resolve every decidable fork to the highest-quality in-scope option and record the rationale; never block on the user.</mandate>
    <mandate>NEVER skip a stage or run them out of order — the sequence 0 &#8594; 5 is strict; each stage consumes the prior stage's emitted context block.</mandate>
  </llm>

  <flow>
    <stage n="0" title="Capability + topology resolution" autonomous="true">
      <load file=".claude/commands/tmux/feature/stage-0-capability.xml">READ stage-0-capability.xml NOW and execute its content as this stage. It is present (shipped with F1).</load>
    </stage>

    <stage n="1" title="Context mapping" autonomous="true">
      <load file=".claude/commands/tmux/feature/stage-1-context.xml">CONDITIONAL: if feature/stage-1-context.xml exists, READ it NOW and execute its content as this stage. If the shard file is NOT present, print "Stage 1 pending (F2&#8211;F6) — shard feature/stage-1-context.xml not yet present" and STOP gracefully (do NOT error).</load>
    </stage>

    <stage n="2" title="Architecture recommendation" interactive="true">
      <load file=".claude/commands/tmux/feature/stage-2-architecture.xml">CONDITIONAL: if feature/stage-2-architecture.xml exists, READ it NOW and execute its content as this stage. If the shard file is NOT present, print "Stage 2 pending (F2&#8211;F6) — shard feature/stage-2-architecture.xml not yet present" and STOP gracefully (do NOT error). This is the SOLE interactive stage: it may prompt the user to confirm the architecture approach.</load>
    </stage>

    <stage n="3" title="Docs + test strategy" autonomous="true">
      <load file=".claude/commands/tmux/feature/stage-3-docs.xml">CONDITIONAL: if feature/stage-3-docs.xml exists, READ it NOW and execute its content as this stage. If the shard file is NOT present, print "Stage 3 pending (F2&#8211;F6) — shard feature/stage-3-docs.xml not yet present" and STOP gracefully (do NOT error).</load>
    </stage>

    <stage n="4" title="Implementation (delegates to /tmux:plan + /tmux:supervisor)" autonomous="true">
      <load file=".claude/commands/tmux/feature/stage-4-implementation.xml">CONDITIONAL: if feature/stage-4-implementation.xml exists, READ it NOW and execute its content as this stage. If the shard file is NOT present, print "Stage 4 pending (F2&#8211;F6) — shard feature/stage-4-implementation.xml not yet present" and STOP gracefully (do NOT error). REUSE /tmux:plan + /tmux:supervisor by reference — never re-implement them.</load>
    </stage>

    <stage n="5" title="Validation (delegates to /tmux:investigate)" autonomous="true">
      <load file=".claude/commands/tmux/feature/stage-5-validation.xml">CONDITIONAL: if feature/stage-5-validation.xml exists, READ it NOW and execute its content as this stage. If the shard file is NOT present, print "Stage 5 pending (F2&#8211;F6) — shard feature/stage-5-validation.xml not yet present" and STOP gracefully (do NOT error). REUSE /tmux:investigate by reference — never re-implement it.</load>
    </stage>

    <!-- ================================================================
         Stages 1&#8211;5 are <load> seams. Each shard file (feature/stage-N-*.xml)
         is authored by a later feature (F2&#8211;F6); until it exists, the stage
         stops gracefully with a "pending" message. The <load> directive is an
         LLM instruction, not an XML include — referencing a not-yet-created
         shard does NOT break the embedded well-formed walk (it walks only
         files that exist on disk).
         ================================================================ -->
  </flow>

  <execution-rules>
    <rule>Stage sequence is strict and one-way: 0 &#8594; 1 &#8594; 2 &#8594; 3 &#8594; 4 &#8594; 5. Never re-order, never skip.</rule>
    <rule>Stage 2 is the ONLY interactive stage; all other stages are autonomous and resolve forks per the highest-quality in-scope option, recording rationale.</rule>
    <rule>COMPOSE existing primitives — delegate implementation to /tmux:plan + /tmux:supervisor and validation to /tmux:investigate; never duplicate their logic.</rule>
    <rule>PORTABILITY: every project-shaped value is resolved from `tmux-cli rules resolve` / `--signals` / manifests or ASKed at Stage 2 — never hardcoded; greenfield defaults only.</rule>
    <rule>A missing future-stage shard stops gracefully with its "pending" message — never crash, never improvise the stage body.</rule>
    <error-reporting>On a genuine harness/infra/spec defect (an MCP tool or `tmux-cli` command that errored after its own retries, a contradictory/under-specified goal spec, or a validator-error-class condition), follow the shared `error-reporting` procedure in `tmux/task-report.xml` — compose + submit autonomously, honoring api.enabled / DEDUP / RECURSION-guard. NEVER on expected work failures (red validate cycle, supervisor pushback, NEED_INPUT, unwritten-code test red) or daemon-owned goal-failures.</error-reporting>
  </execution-rules>

  <llm final="true">
    <critical-rules>
      * COMPOSER, not engine — delegate to /tmux:plan, /tmux:supervisor, /tmux:investigate; never re-implement them
      * PORTABILITY — resolve every project-shaped value from `tmux-cli rules resolve`/`--signals`/manifests or ASK; greenfield defaults only; no project name anywhere
      * Interactive ONLY at Stage 2 — Stages 0/1/3/4/5 are autonomous
      * Strict stage order 0 &#8594; 5 — each stage consumes the prior stage's emitted context block
      * A missing future-stage shard STOPS gracefully with its "pending" message — never crash
    </critical-rules>
  </llm>
</task>
