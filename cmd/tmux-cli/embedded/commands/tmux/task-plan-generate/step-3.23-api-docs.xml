    <step n="3.23" title="Generate API documentation goal (phase=api_docs) — CONDITIONAL">
      <rule critical="true">This step is CONDITIONAL — skip entirely if OpenAPI/nelmio was not selected during discovery.</rule>
      <rule critical="true">All inputs read from docs/architecture/* files on disk (G-16), never from conversation context.</rule>

      <substep n="3.23.1" title="Check OpenAPI selection in discovery output">
        <action>Read docs/architecture/cross-cutting.md. Locate the Operational Concerns section.</action>
        <action>Search for indicators that OpenAPI/nelmio/api-doc-bundle was selected: keywords "OpenAPI", "nelmio", "api-doc-bundle", "Swagger", or equivalent.</action>
        <check condition="OpenAPI/nelmio NOT found in Operational Concerns">
          <action>Print: "API documentation not selected — skipping step 3.23"</action>
          <action>SKIP entire step — proceed to next step.</action>
        </check>
        <action>If found, store API_DOC_FORMAT = detected format description (e.g., "OpenAPI 3.0 via nelmio/api-doc-bundle").</action>
        <action>Print: "API documentation selected: {{API_DOC_FORMAT}} — proceeding with goal generation"</action>
      </substep>

      <substep n="3.23.2" title="Resolve dependency goal IDs">
        <action>Read goals.yaml from disk. Filter all goal entries where phase=action. Collect their goal IDs into ACTION_GOAL_IDS list.</action>
        <action>Filter all goal entries where phase=auth. Collect their goal IDs into AUTH_GOAL_IDS list.</action>
        <action>Set API_DOC_DEPENDS_ON = ACTION_GOAL_IDS + AUTH_GOAL_IDS.</action>
        <check condition="ACTION_GOAL_IDS is empty">
          <action>ABORT with error: "No controller action goals found in goals.yaml (phase=action) — cannot generate API docs goal without endpoints to document"</action>
        </check>
        <action>Print: "API docs depends on {{count(ACTION_GOAL_IDS)}} action goals + {{count(AUTH_GOAL_IDS)}} auth goals"</action>
        <rule>AUTH_GOAL_IDS may be empty if the project has no auth flows — that is valid. ACTION_GOAL_IDS must never be empty.</rule>
        <rule>This MUST be dynamic — goal counts vary per project. Never hardcode goal IDs.</rule>
      </substep>

      <substep n="3.23.3" title="Build acceptance criteria array">
        <action>Build ACCEPTANCE array from AD-01..AD-04:</action>
        <criteria>
          <c id="AD-01">OpenAPI spec generated at configured endpoint (e.g., /api/doc.json) — curl endpoint, valid JSON returned</c>
          <c id="AD-02">Every controller action documented in OpenAPI spec — count operations in spec matches action count</c>
          <c id="AD-03">Request/response schemas match actual DTOs — compare spec schemas with DTO properties</c>
          <c id="AD-04">OpenAPI spec validates against OpenAPI 3.0 — generated by `bin/console nelmio:apidoc:dump --format=json` without errors (PHP-native, runs in the app container; NO Node/npx/swagger-cli required). Use a Node validator (swagger-cli) ONLY when Node is available per NODE-TOOL-CONV.</c>
        </criteria>
      </substep>

      <substep n="3.23.4" title="Build CONTEXT string with Investigation Config">
        <action>Build CONTEXT string with three sections: Purpose, Deliverables, Investigation Config.</action>
        <action>Purpose: "Configure {{API_DOC_FORMAT}} for all controller endpoints. Generate valid OpenAPI 3.0 spec with complete annotations."</action>
        <action>Deliverables: nelmio/api-doc-bundle configuration (nelmio_api_doc.yaml), OpenAPI annotations on all controller actions, schema definitions for request/response DTOs, OpenAPI endpoint route registration.</action>

        <section name="investigation-config">
          <action>Build 3 investigators:</action>

          <investigator n="1" name="OpenAPI endpoint reachable">
            <type>environment-check</type>
            <commands>curl -sf {base_url}/api/doc.json | python3 -m json.tool &gt; /dev/null</commands>
            <pass>Endpoint returns valid JSON — OpenAPI spec is served</pass>
            <fail>Endpoint unreachable, returns error, or invalid JSON</fail>
          </investigator>

          <investigator n="2" name="Operation coverage">
            <type>file-check</type>
            <commands>curl -sf {base_url}/api/doc.json | python3 -c "import sys,json; spec=json.load(sys.stdin); ops=sum(len(m) for p in spec.get('paths',{}).values() for m in [p] if isinstance(p,dict)); print(f'Operations: {ops}')"</commands>
            <pass>Operation count matches total controller action count (ACTION_COUNT + N_auth_flows)</pass>
            <fail>Operation count mismatch — some endpoints undocumented</fail>
          </investigator>

          <investigator n="3" name="Schema validation">
            <type>quality-gate</type>
            <commands>bin/console nelmio:apidoc:dump --format=json &gt; /dev/null</commands>
            <pass>Exit 0 — the bundle renders a structurally valid OpenAPI 3.0 spec (PHP-native; no Node required)</pass>
            <fail>Dump fails — invalid annotations or unresolved schema references</fail>
          </investigator>

          <rule>3 investigators for API docs goal (GM-08 minimum is 2). Each has type, commands, pass/fail criteria (GM-09).</rule>
        </section>
      </substep>

      <substep n="3.23.5" title="Call goal-create for API documentation goal">
        <action>Call goal-create MCP tool:</action>

        <params>
          <param name="description">API docs: configure nelmio/api-doc-bundle with OpenAPI annotations and schema validation</param>
          <param name="acceptance">ACCEPTANCE array from substep 3.23.3 (AD-01..AD-04 — 4 items)</param>
          <param name="validate">[
            "curl -sf {base_url}/api/doc.json | python3 -m json.tool",
            "vendor/bin/phpstan analyse contexts/ projects/ packages/ --level=9",
            "vendor/bin/ecs check contexts/ projects/ packages/",
            "vendor/bin/deptrac analyse"
          ]</param>
          <param name="context">CONTEXT string from substep 3.23.4 (purpose + deliverables)</param>
          <param name="investigation_config">Array of {name,type,paths,commands,pass,fail,condition?} objects built from substep 3.23.4's investigators (2-4; types from the Reference Table). Omit conditional investigators whose condition is false.</param>
          <param name="not_in_scope">Controller implementation (built by action goals), auth endpoints (built by auth goals), domain/application/infrastructure layers, DTO creation (built by per-BC goals), database migrations, frontend UI.</param>
          <param name="depends_on">API_DOC_DEPENDS_ON — ALL action + auth goal IDs collected in substep 3.23.2</param>
          <param name="max_retries">3</param>
        </params>

        <action>After successful goal-create, record the returned goal ID as API_DOC_GOAL_ID.</action>

        <check condition="goal-create returns error">
          <action>Print error with full context and ABORT.</action>
        </check>

        <rule>description must be &lt;=120 characters (MaxGoalDescriptionLength invariant).</rule>
        <rule>max_retries=3 per G-11.</rule>
        <rule>phase=api_docs on the created goal.</rule>
      </substep>

      <substep n="3.23.6" title="Record API documentation goal ID">
        <action>Store API_DOC_GOAL_ID for use by downstream steps.</action>
        <action>Print summary: "API documentation goal created: 1 goal (phase=api_docs, depends_on={{count(API_DOC_DEPENDS_ON)}} goals [{{count(ACTION_GOAL_IDS)}} action + {{count(AUTH_GOAL_IDS)}} auth]), ID: {{API_DOC_GOAL_ID}}"</action>
        <action>Proceed to next step.</action>
      </substep>
    </step>
