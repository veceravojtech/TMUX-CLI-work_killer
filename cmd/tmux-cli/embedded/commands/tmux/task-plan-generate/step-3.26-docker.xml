    <step n="3.26" title="Generate Docker/deployment goal (conditional, phase=docker)">
      <rule critical="true">This step is CONDITIONAL and PRODUCTION-DEPLOYMENT-ONLY — skip unless a Docker/container/deployment ADR exists in docs/architecture/adrs/. The DEV runtime container (docker-compose.yaml used for development + the in-container PHP toolchain) is now created EARLY by goal-002 (step 2 DOCKER-RUNTIME-FRONTLOAD), so RUN_TARGET=docker ALONE no longer fires this goal — only a deployment ADR does. When it fires it produces PRODUCTION deployment extras (multi-stage prod Dockerfile, prod compose, .env.example) as late deliverables that nothing else depends on.</rule>
      <rule critical="true">DECOUPLED triggers, post-front-load: "run in a container for development" is handled by goal-002 task-R (early). This step is ONLY "production deployment" (Docker/deployment ADR). A project with RUN_TARGET=docker but no deployment ADR gets its dev runtime from goal-002 and SKIPS this step.</rule>
      <rule critical="true">All inputs read from docs/architecture/* files on disk (G-16), never from conversation context.</rule>
      <rule critical="true">depends_on MUST include ALL previous goal IDs from goals.yaml — Docker goal depends on the entire project (G-03).</rule>

      <substep n="3.26.1" title="Determine compose trigger (RUN_TARGET=docker OR deployment ADR)">
        <action>Read docs/architecture/test-environment.md from disk. Set RUN_TARGET: test-environment.md "Run Target" == docker → set RUN_TARGET=docker, else RUN_TARGET=local. (IDENTICAL rule to substep 1.2/step 0b — do not drift. If test-environment.md is absent, treat RUN_TARGET=local.)</action>
        <action>Scan docs/architecture/adrs/ directory. Read all ADR files. Search for Docker/container/deployment keywords: "docker", "dockerfile", "docker-compose", "container", "containerization", "deployment". Set ADR_PRESENT=true if any ADR matches, else false.</action>
        <action>Set COMPOSE_TRIGGER = ADR_PRESENT. (Post-front-load: RUN_TARGET=docker no longer triggers this step — the dev runtime is created by goal-002 task-R. Only a deployment ADR fires production deployment extras here.)</action>
        <check condition="NOT ADR_PRESENT (COMPOSE_TRIGGER is false)">
          <action>Print: "No Docker/deployment ADR — skipping step 3.26 (dev runtime already created by goal-002 task-R)"</action>
          <action>SKIP entire step — proceed to next step.</action>
        </check>
        <action>DOCKER_ADR_SUMMARY is OPTIONAL: only when ADR_PRESENT, store DOCKER_ADR_SUMMARY = summary of Docker decisions from the matching ADR (base image, compose services, port mappings, prod extras). When RUN_TARGET=docker without an ADR, leave DOCKER_ADR_SUMMARY unset — base image/services derive from architecture files (substep 3.26.3).</action>
        <action>Print: "Compose goal firing — trigger: RUN_TARGET={{RUN_TARGET}}, ADR_PRESENT={{ADR_PRESENT}} (ONE goal; ADR, if present, adds prod extras)"</action>
      </substep>

      <substep n="3.26.2" title="Collect all previous goal IDs">
        <action>Read goals.yaml from disk. Collect ALL goal IDs (every entry, regardless of phase) into ALL_PREV_GOAL_IDS list.</action>
        <check condition="ALL_PREV_GOAL_IDS is empty">
          <action>ABORT with error: "No goals found in goals.yaml — cannot generate Docker goal without prior goals"</action>
        </check>
        <action>Print: "Docker goal depends on {{count(ALL_PREV_GOAL_IDS)}} previous goals (all phases)"</action>
        <rule>This MUST be a full scan of goals.yaml — not filtered by phase. Docker depends on ALL prior goals because Dockerfile/compose must account for every service, BC, and infrastructure component.</rule>
      </substep>

      <substep n="3.26.3" title="Read architecture inputs">
        <action condition="ADR_PRESENT">Read the matching ADR file(s) for specific Docker decisions: base image selection, compose service definitions, port mappings, volume mounts, production extras.</action>
        <action>Read docs/architecture/test-environment.md for service list and connection strings (GM-05 — never hardcode credentials).</action>
        <action>Parse the "Published Ports" block in test-environment.md (the per-service table: Service | Host Port | Container Port | Purpose, persisted by discovery / D1a) into PUBLISHED_PORTS — a per-service host:container mapping keyed by service name.</action>
        <action>Set APP_PORT = the host port of the app/web service in PUBLISHED_PORTS. App/web service heuristic: the service whose container port matches the framework's HTTP port; when ambiguous, the service named app/web/php in cross-cutting.md (document the chosen service in goal CONTEXT so the worker picks deterministically).</action>
        <action>FALLBACK when the Published Ports block is ABSENT: set APP_PORT = the port from the base URL in test-environment.md, and record a context note that ports were defaulted from the base URL (surface as a RISK in goal CONTEXT).</action>
        <action>Set BASE_IMAGE = from DOCKER_ADR_SUMMARY when ADR_PRESENT, else the runtime/framework default recorded in test-environment.md/cross-cutting.md (NEVER hardcoded — GM-05).</action>
        <action>Read docs/architecture/cross-cutting.md for infrastructure decisions (Redis, RabbitMQ, database, etc.) that must appear as services in docker-compose.yaml.</action>
        <action>Store INFRA_SERVICES = list of infrastructure services discovered (DB, Redis, RabbitMQ, etc.).</action>
      </substep>

      <substep n="3.26.4" title="Compose acceptance criteria (DK-01..DK-05)">
        <action>Build ACCEPTANCE array from DK-01..DK-05:</action>
        <criteria>
          <c id="DK-01">Dockerfile builds successfully — docker build . exit code 0</c>
          <c id="DK-02">docker-compose.yaml includes each service (app + DB, Redis, etc.) with its host:container port mapping from the Published Ports block (PUBLISHED_PORTS) — read compose file, every service and mapping match</c>
          <c id="DK-03">docker compose up starts all services — services running after up</c>
          <c id="DK-04">Application accessible at the published host port APP_PORT (from PUBLISHED_PORTS) — curl localhost:$APP_PORT, response received</c>
          <c id="DK-05">.env.example lists all required environment variables — compare with actual .env needs</c>
        </criteria>
      </substep>

      <substep n="3.26.5" title="Compose validation commands">
        <action>Build VALIDATE array:</action>
        <validate>
          <v>docker build .</v>
          <v>docker compose config --quiet</v>
          <v>docker compose up -d --wait &amp;&amp; curl -sf http://localhost:$APP_PORT/health</v>
        </validate>
        <rule>$APP_PORT is the host port of the app/web service from the Published Ports block (PUBLISHED_PORTS) in docs/architecture/test-environment.md — never hardcoded (GM-05); falls back to the base-URL port when the block is absent (substep 3.26.3).</rule>
      </substep>

      <substep n="3.26.6" title="Compose context with Investigation Config">
        <action>Build CONTEXT string with three sections: Purpose, Deliverables, Investigation Config.</action>
        <action>Purpose: "Create Docker deployment artifacts for the project. Dockerfile for application image, docker-compose.yaml for local development with all infrastructure services ({{INFRA_SERVICES}}), and .env.example documenting all required environment variables."</action>
        <action>Deliverables: Dockerfile (multi-stage build for production), docker-compose.yaml (app + all infrastructure services), .env.example (all required env vars with placeholder values).</action>

        <section name="investigation-config">
          <action>Build 3 investigators:</action>

          <investigator n="1" name="Docker build check">
            <type>environment-check</type>
            <commands>docker build -t app-test . &amp;&amp; echo "BUILD_OK"</commands>
            <pass>Exit 0, BUILD_OK printed — Dockerfile builds successfully</pass>
            <fail>Build fails — missing dependencies, invalid instructions, or copy errors</fail>
          </investigator>

          <investigator n="2" name="Compose services check">
            <type>file-check</type>
            <commands>docker compose config --services</commands>
            <pass>All expected services listed (app + infrastructure services from ADRs)</pass>
            <fail>Missing services or invalid compose configuration</fail>
          </investigator>

          <investigator n="3" name="Application accessibility check">
            <type>integration-check</type>
            <commands>docker compose up -d --wait &amp;&amp; curl -sf http://localhost:$APP_PORT/health &amp;&amp; docker compose down</commands>
            <pass>Health endpoint responds — application accessible at the published host port APP_PORT</pass>
            <fail>Application not reachable, health check fails, or services crash on startup</fail>
          </investigator>

          <rule>3 investigators for Docker goal (GM-08 minimum is 2). Each has type, commands, pass/fail criteria (GM-09).</rule>
        </section>
      </substep>

      <substep n="3.26.7" title="Call goal-create MCP">
        <action>Call goal-create MCP tool:</action>

        <params>
          <param name="description">Create Dockerfile, docker-compose.yaml, and .env.example for deployment</param>
          <param name="acceptance">ACCEPTANCE array from substep 3.26.4 (DK-01..DK-05 — 5 items)</param>
          <param name="validate">VALIDATE array from substep 3.26.5 (3 commands)</param>
          <param name="context">CONTEXT string from substep 3.26.6 (purpose + deliverables)</param>
          <param name="investigation_config">Array of {name,type,paths,commands,pass,fail,condition?} objects built from substep 3.26.6's investigators (2-4; types from the Reference Table). Omit conditional investigators whose condition is false.</param>
          <param name="not_in_scope">Application code implementation (built by domain/infra/action goals), CI/CD pipeline configuration, production deployment orchestration (Kubernetes, Swarm), cloud provider setup, SSL/TLS configuration.</param>
          <param name="depends_on">ALL_PREV_GOAL_IDS — ALL goal IDs collected in substep 3.26.2</param>
          <param name="max_retries">3</param>
        </params>

        <action>After successful goal-create, record the returned goal ID as DOCKER_GOAL_ID.</action>

        <check condition="goal-create returns error">
          <action>Print error with full context and ABORT.</action>
        </check>
        <check condition="goal-create succeeds">
          <action>Print: "Docker/deployment goal created: {{DOCKER_GOAL_ID}} (phase=docker, depends_on={{count(ALL_PREV_GOAL_IDS)}} goals [all phases]) with 3 investigators, single-task fan-out, max_retries=3"</action>
          <action>Proceed to next step.</action>
        </check>

        <rule>description is 70 characters — within MaxGoalDescriptionLength (120) invariant.</rule>
        <rule>depends_on MUST be the FULL ALL_PREV_GOAL_IDS list — never subset or phase-filtered (G-03).</rule>
        <rule>max_retries=3 per G-11.</rule>
        <rule>phase=docker on the created goal.</rule>
      </substep>
    </step>
