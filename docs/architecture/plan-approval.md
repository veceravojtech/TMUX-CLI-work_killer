# Blind Plan Audit — Pass 2 APPROVAL — .tmux-cli/tasks.yaml (plan dispatch 2026-07-02-10)

- **Date:** 2026-07-02
- **Auditor:** blind plan audit, pass 2 (read-only; no prior context beyond the pass-1 ledger)
- **Plan:** 3 tasks (`execute-1` repair self-reinstall, `execute-2` dispatcher self-update lane, `execute-3` e2e report writer), status: ready
- **Score:** 91 / 100 (start 100; 3× SEV-4 open −9; F1 SEV-3 resolved, no deduction)
- **Verdict:** **PASS** (score ≥ 90 AND zero open SEV-1/SEV-2)

## Prior-findings resolution (vs docs/architecture/plan-audit-1.md)

| # | Severity | Status | Evidence |
|---|----------|--------|----------|
| F1 | SEV-3 (−8) | **RESOLVED** | `execute-1-repair-self-reinstall.md:124-126` rewritten: the probe now uses `--build-cmd "touch $HOME/.local/bin/tmux-cli"` with a throwaway `--project` (`mktemp -d`) and `test -f "$tmp/.tmux-cli/taskvisor-restart"`. Re-verified against source: the touch trips `binaryChanged` (`cmd/tmux-cli/self_update.go:92-101`), `binary_changed:true` reaches the `restartDaemon` marker write (`:284-294`), the `strings.Fields` no-shell caveat (`:363-366`) and the live-daemon-adoption/fake-`HOME` isolation caveats are accurate, and the `&&` chain surfaces failure (no failure-hiding). The Spec Change Log records the round; all pass-1 KEEP items verified un-regressed (marker-placement Always rule + AC-5 + `TestMaybeSelfReinstall_ProjectIsBaseDir`, insertion point before the deferred-validation fork at `statemachine.go:408-410`, per-cycle stamp + `ResetGoal` clear, non-destructive build failure, worktree-as-source mandate, all anchors intact). |
| F2 | SEV-4 (−3) | **OPEN** | `execute-2-dispatcher-selfupdate-lane.md:127` (and the harness sketch at `:89`) is byte-identical to pass 1 — "Run the TC suite; grep `dispatcher.log` …" names no concrete runnable harness; sourcing `dispatcher.sh` as-is still executes `${1:?}` (`dispatcher.sh:10`) and the main loop. Spec Change Log: "(empty — initial version)". Mitigations from pass 1 still hold (TC-1..TC-10 behaviorally precise, sandbox fully described). |
| F3 | SEV-4 (−3) | **OPEN** | `execute-2-dispatcher-selfupdate-lane.md:20` rationale (a) still relies on the self-target refusal (`self_update.go:73-75`) firing on the cli lane, which execute-1 relaxes for cli checkouts. Spec-1 added the ordering-visibility note (`execute-1-repair-self-reinstall.md:115`) acknowledging the hazard-based rationale should bind, but the cited spec-2 text was never reworded, so the documentation-level cross-spec inconsistency persists. Still no behavioral/acceptance contradiction — rationale (b) independently mandates `--project "$WORKER_HOME"` under both orderings. |
| F4 | SEV-4 (−3) | **OPEN** | `execute-3-e2e-report-writer.md:147` unchanged: `go build ./... && ./bin/tmux-cli e2e-state report --help 2>/dev/null \|\| true` — the trailing `\|\| true` makes the check unable to fail, and `go build ./...` does not produce `./bin/tmux-cli` (only `make build` writes `bin/`, per Makefile/AGENTS.md). Spec Change Log: "(empty — initial submission)". Mitigated: the flag surface is redundantly pinned by `TestReport_ReportCommandMandate` and the cmd-layer tests. |

## Per-dimension summary

1. **validate-executability** — All spec-1 and spec-2 probes re-verified as concrete bare commands with exit-code-visible failure, including the rewritten F1 touch-probe (verified against `self_update.go` code paths) and spec-2's two live probes (`--dry-run` exit-0 single-JSON-line; `--build-cmd false` → rc=1, no stdout JSON — both re-derived from `runSelfUpdate`/`doSelfUpdate`, exit-0-only JSON at `self_update.go:394-403`). Deductions: F2, F4 (unchanged from pass 1).
2. **dependency-correctness** — tasks.yaml declares no `depends_on` and needs none: no cycles/dangling refs; execute-2 is a pure `../dispatcher` consumer whose AC-5 (`--project "$WORKER_HOME"`) holds both before and after execute-1's guard relaxation; execute-1 and execute-3 touch disjoint files/symbols. Clean.
3. **runtime-state-gating** — daemon behavior tested via injectable `selfUpdateFn` (mirrors `execReplaceFn`, `daemon.go:255`) and mocks; spec-1's revised probe now explicitly gates its live side effects (fake `HOME` + throwaway `--project` so a live daemon never adopts an unplanned restart) — an improvement over pass 1. Only F2's forced-cycle grep remains under-gated (counted in F2). Clean otherwise.
4. **host-container-split** — everything host-side; spec-2 names the sibling repo path and deploy host explicitly. No container assumptions. Clean.
5. **objective-acceptance** — all acceptance criteria across the three specs remain Given/When/Then with machine-checkable observables (file existence, exit codes, JSON fields, grep-able log lines, mock argv, byte-identity diffs). Clean.
6. **spec-discovery-consistency** — independently re-verified ~30 anchors this pass, all matching: `statemachine.go` :337/:396/:408-410/:415/:420/:1433-1437/:1508-1531; `daemon.go` :255/:270/:327/:454-473/:591; `self_update.go` :73-75/:92-101/:105-122/:142-152/:274-276/:284-294/:322-328/:363-366/:373/:394-403/:417-418; `dispatcher.sh` :11/:39-44/:98-140/:106-108/:110/:111-119/:124-127/:129/:132-134/:139/:144-150/:192/:204; `internal/e2e/e2e.go` :147/:162/:167-185/:221/:234/:285-310/:351/:539; `e2e_state.go` :44-47/:106-121/:217-224/:276-310; `e2e-evaluator.xml` :10/:36/:92/:222-227/:233/:239-248/:276/:282/:286; pin tests :45-52/:150/:176-184/:203-210 and the `.claude` mirror byte-identity (:214-222; `diff` clean live). The pass-1 sole mismatch (F1's probe claim) is fixed and now matches source. Clean.
7. **environment-prerequisites** — go/make/git/bash/flock/tmux given by the repo workflow; `../dispatcher` exists at the stated path; files-to-create (`selfsource.go`, `selfreinstall.go`, `report.go`) confirmed absent; `mktemp`/`touch` are POSIX-standard; jq deliberately avoided. Clean.
8. **scope-sanity** — each spec stays in its declared boundary (spec-1's `self_update.go` touch is the authorized thin-hook escape hatch; spec-2 changes only `dispatcher.sh`; spec-3 preserves XML step numbering, SLOT comments, `<error-reporting>` reference). Deduction: F3 (stale cross-spec rationale in spec-2, doc-level only).
9. **rule-coverage** — `code-rules.md` records the deterministic match per task; all three report "no code rules match" and this reproduces live this pass (`tmux-cli rules match` → "no code rules match the given files", including for files the recorded queries omitted). Trivially satisfied.

## Scoring

Start 100 → F1 resolved (0) − F2 (3) − F3 (3) − F4 (3) = **91**. No open SEV-1/SEV-2. PASS gate (≥90, zero SEV-1/2) met.

Residual risk of the three open SEV-4s is executor-inconvenience only (a vague harness line, a stale doc rationale, and a can-never-fail optional check redundantly covered by pin tests); none can produce a false-pass of an acceptance criterion or misdirect an implementation.
