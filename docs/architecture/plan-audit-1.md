# Blind Plan Audit — .tmux-cli/tasks.yaml (plan dispatch 2026-07-02-10)

- **Date:** 2026-07-02
- **Auditor:** blind plan audit (read-only; no prior context)
- **Plan:** 3 tasks (`execute-1` repair self-reinstall, `execute-2` dispatcher self-update lane, `execute-3` e2e report writer), status: ready
- **Score:** 83 / 100 (start 100; 1× SEV-3 −8, 3× SEV-4 −9)
- **Verdict:** FAIL (PASS requires score ≥ 90; zero SEV-1/SEV-2 findings are open, the failure is on score alone)

## Findings

| # | Severity | Dimension | File:Line | Description |
|---|----------|-----------|-----------|-------------|
| F1 | SEV-3 (−8) | validate-executability, spec-discovery-consistency | `.tmux-cli/research/2026-07-02-10/execute-1-repair-self-reinstall.md:123` | The "Optional fast probe" `tmux-cli self-update --source <worktree> --project <base> --restart daemon --build-cmd "true"` claims to "exercise marker placement under `<base>/.tmux-cli/` cheaply". It cannot: with a no-op build the installed binary's size/mtime are unchanged, and `doSelfUpdate` returns before the restart switch when `!changed` (`cmd/tmux-cli/self_update.go:274-276`), so `restart:"none"` is reported and **no `taskvisor-restart` marker is ever written**. This also contradicts the spec's own I/O matrix row "No-op rebuild → `binary_changed:false, restart:"none"` → no marker" (spec line 38). An executor using this probe to verify acceptance criterion 5 (marker lands under the base project) will observe no marker and can burn a debug/retry loop on a correct implementation. |
| F2 | SEV-4 (−3) | validate-executability, runtime-state-gating | `.tmux-cli/research/2026-07-02-10/execute-2-dispatcher-selfupdate-lane.md:127` (also :89) | Verification bullet "Run the TC suite; grep `dispatcher.log` for the preserved `cli:` lines after one forced update cycle (`UPDATE_CHECK_INTERVAL=0`)" is not a concrete, bare, runnable command. The dispatcher repo has no test harness or runner convention, and the Test Plan's harness strategy ("sourcing `dispatcher.sh` with a guard") requires an unstated source-guard modification — sourcing the script as-is executes `${1:?}` and the infinite main loop (`dispatcher.sh:154`). Mitigated: TC-1..TC-10 are behaviorally precise, the sandbox (stub tmux-cli + throwaway CLI_SRC) is fully described, and the forced-cycle grep can plausibly be satisfied against the sandbox `$LOG`. |
| F3 | SEV-4 (−3) | scope-sanity (cross-spec consistency) | `.tmux-cli/research/2026-07-02-10/execute-2-dispatcher-selfupdate-lane.md:20` vs `execute-1-repair-self-reinstall.md:22` | Spec-2's Ask-First #1 rationale (a) relies on the self-target refusal (`self_update.go:73-75`) firing when `PROJECT_DIR == CLI_SRC` on the cli lane. Spec-1 relaxes exactly that refusal for cli source checkouts (`CLI_SRC` is one), so once execute-1 lands, rationale (a) is stale — the refusal would no longer fire. No behavioral or acceptance-level contradiction: rationale (b) (a lane-dir marker misreads the next daemon start as a planned exec-replace and auto-activates, `daemon.go:454-473`) independently mandates `--project "$WORKER_HOME"` under both task orderings. Documentation-level inconsistency only. |
| F4 | SEV-4 (−3) | validate-executability | `.tmux-cli/research/2026-07-02-10/execute-3-e2e-report-writer.md:147` | Verification line `go build ./... && ./bin/tmux-cli e2e-state report --help 2>/dev/null \|\| true` is a chained command that hides failures: the trailing `\|\| true` makes it exit 0 unconditionally, and `go build ./...` does not produce `./bin/tmux-cli` (only `make build` writes `bin/`), so the help invocation can fail silently or run a stale binary. As written the check can never fail and asserts nothing ("help lists all 10 flags" is never grepped). Mitigated: the flag surface is redundantly covered by the cmd-layer tests and pin suite. |

## What was verified clean (per dimension)

1. **validate-executability** — all other verification commands across the three specs are concrete bare commands with stated expected outcomes (`go test …`, `grep -n …`, `bash -n bin/dispatcher.sh`, `diff` of the XML mirror, `make install`, the two real `self-update --dry-run`/`--build-cmd false` probes whose expected JSON/exit codes were re-derived from `self_update.go` and match exactly). Deductions: F1, F2, F4.
2. **dependency-correctness** — tasks.yaml declares no `depends_on` edges and none are needed: no cycles, no dangling refs; execute-2 is a pure `../dispatcher` consumer that deliberately does NOT depend on execute-1's guard relaxation (it routes `--project` to `$WORKER_HOME`); execute-1 and execute-3 touch disjoint files/symbols (`self_update.go`+taskvisor+setup vs `e2e_state.go`+`internal/e2e`+XML). Clean.
3. **runtime-state-gating** — daemon-dependent behavior is tested via injectable `selfUpdateFn`/mocks, not a live daemon; spec-2's verification states its host gate ("host with tmux-cli installed"); spec-3's smoke is scoped to a temp dir bootstrapped by `e2e-bootstrap`. Only the F2 forced-cycle grep is under-gated. Clean otherwise.
4. **host-container-split** — everything runs on the host; spec-2 explicitly names the sibling-repo path and the deploy host (`ubuntu-8gb-nbg1-2` per dispatcher AGENTS.md). No container assumptions. Clean.
5. **objective-acceptance** — all acceptance criteria in all three specs are Given/When/Then with machine-checkable observables (file existence/bytes, exit codes, JSON fields, grep-able log lines, mock argv). Clean.
6. **spec-discovery-consistency** — ~40 file:line anchors were verified against source and match exactly or within ±2 lines, including: `statemachine.go` :337/:393/:409/:415/:420/:1433/:1508/:1516/:1531; `daemon.go` :255/:270/:423-424/:454-473/:591; `goals.go` :87/:134/:147/:189/`ResetGoal`:706; `self_update.go` :58/:73-75/:105-122/:142-152/:256-259/:283-294/:322-328/:373/:394-403/:417-418; `worktree.go` :1056/:1166; `dispatch.go` :71-88; `recovery.go` :101-122; `dispatcher.sh` :11/:39-44/:98-140/:106-108/:110/:111-119/:124-127/:129/:132-134/:139/:144-150/:192/:204; `Makefile` :71-74/:130-134; `internal/e2e/e2e.go` :147/:162/:221/:234/:285/:289/:304/:351/:539; `e2e_state.go` :44-47/:106-121/:217-224/:276-310; `e2e-evaluator.xml` :10/:36/:92/:222-227/:233/:239-248/:276/:282/:286; pin tests :45-61/:52/:150-170/:176-184/:203-222 and the `.claude` mirror byte-identity (verified in sync). Behavioral claims (exit-0-only JSON, `binary_changed` gating of restart dispatch, dry-run resolving the source guard, marker write path, `clearReports` legacy sweep, `go.mod` module path) all re-derived and confirmed. Sole mismatch is F1's probe claim.
7. **environment-prerequisites** — go/make/git/bash/flock/tmux are given by the repo's own workflow; `../dispatcher` exists at the stated path; jq is deliberately avoided; the specs' files-to-create (`selfsource.go`, `selfreinstall.go`, `report.go`) do not yet exist. Clean.
8. **scope-sanity** — each spec stays within its declared boundary; execute-1's `self_update.go` touch is the explicitly-authorized "thin unavoidable hook" escape hatch from its task context; execute-2 changes only `dispatcher.sh`; execute-3's XML edits preserve step numbering, SLOT comments, and the `<error-reporting>` reference. Deduction: F3 (stale cross-spec rationale, not a behavioral contradiction).
9. **rule-coverage** — `code-rules.md` records the deterministic match per task; all three report "no code rules match" and this reproduces live (`tmux-cli rules match` returns empty even for files the recorded queries omitted, e.g. `self_update.go`, `daemon.go`). Trivially satisfied.

## Remediation priority

1. **F1 (execute-1:123)** — fix the optional probe so it actually exercises marker placement, e.g. use a `--build-cmd` that touches the installed binary (`--build-cmd "touch $HOME/.local/bin/tmux-cli"`) or drop the "exercises marker placement" claim and state it only proves the flag surface (like spec-2's dry-run probe). Highest priority: it is the only finding that can send an executor into a false-negative debug loop against its own I/O matrix.
2. **F4 (execute-3:147)** — replace with a failure-surfacing check, e.g. `make build` then `./bin/tmux-cli e2e-state report --help` asserted via grep on the 10 flag names (or drop the line — the pin/cmd tests already cover it).
3. **F2 (execute-2:127, :89)** — pin the TC harness: name a test script path in the dispatcher repo, state that a source-guard line is added to `dispatcher.sh` (it is the file under change), and say explicitly that the forced-update-cycle grep runs against the sandbox `$LOG`.
4. **F3 (execute-2:20)** — reword rationale (a) to "on a pre-relaxation binary the refusal fires; post execute-1 the marker/auto-activate hazard (b) remains the binding reason" so the recorded decision stays true regardless of task ordering.

Re-audit after fixes: all four are spec-text edits; no code or tasks.yaml changes required. Expected post-fix score: 100.
